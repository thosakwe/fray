class FunctionalWrapper {
    let source;

    constructor(source) {
        this.source = source;
    }

    fn sum() {
        let s = 0;

        for (i : this.source) {
            s += i;
        }

        ret s;
    }
}

fn src(x) => new FunctionalWrapper(x);